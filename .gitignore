# 1. Dependencies (สำคัญมาก ห้ามเอาขึ้นเพราะขนาดใหญ่)
node_modules/

# 2. Expo specific (ไฟล์ cache และ config ชั่วคราวของ Expo)
.expo/
*.jks
*.p12
*.key
*.mobileprovision

# 3. Build Outputs (ไฟล์ที่ได้จากการ Build ไม่ใช่ Source code)
dist/
web-build/

# 4. Environment Variables (สำคัญที่สุดเรื่องความปลอดภัย!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 5. OS files (ไฟล์ขยะของระบบปฏิบัติการ)
.DS_Store
Thumbs.db

# 6. Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*